# Workflows

Primary documented path: `booking_fulfillment` triggered by `POST /bookings` and `booking.created`.

See `services/workflow_engine/tasks.py` for the orchestrated step sequence and retry behavior.
