This folder is a minimal Next.js + Tailwind shell for a future operator dashboard (Phase 2 in `.txt`). The active implementation for this milestone is the FastAPI backend under `apps/api`.

To scaffold locally when you are ready:

```bash
cd apps/web
npm install
npm run dev
```
